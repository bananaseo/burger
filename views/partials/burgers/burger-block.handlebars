<li>
	{{burger_name}}

	<button class="change-eaten" data-id="{{id}}" data-newburger="{{eaten}}">
		{{#if eaten}}Devoured!{{else}}Eat!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
